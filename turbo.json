{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build:tsdown": {
      "outputs": ["./dist/**"]
    },
    "build:docs": {
      "dependsOn": ["build:tsdown"],
      "outputs": ["./docs/**"]
    },
    "lint:eslint": {
      "cache": false
    }
  }
}
